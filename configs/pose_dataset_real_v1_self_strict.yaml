# Strict-clean YOLO-pose config generated by filter_yolo_pose_dataset_by_validator.py.
path: data/wheel_pose_dataset_real_v1_self_strict
train: images/train
val: images/val

names:
  0: wheel

kpt_shape: [3, 3]
flip_idx: [1, 0, 2]
